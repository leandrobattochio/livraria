namespace Livraria.Core.Extensions;

public static class ExtendedClaimTypes
{
    public const string UserId = nameof(UserId);
    public const string UserRole = nameof(UserRole);
    public const string ClicheriaId = nameof(ClicheriaId);
    public const string CompanyId = nameof(CompanyId);
}